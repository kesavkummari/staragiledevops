pipeline {
  agent any

  stages {
    stage('Packer - Build Ubuntu Image') {
      steps {
        sh """
        #!/bin/bash
        cd ${WORKSPACE}/staragiledevops/packer
        packer init .
        packer build -force .
        """
      }
    }
  }
}